export const { default: HomeBannerTitle } = require("./HomeBannerTitle");
export const { default: HomeBannerHeading } = require("./HomeBannerHeading");
export const {
  default: HomeBannerGroupButton,
} = require("./HomeBannerGroupButton");
export const { default: HomeBannerDesc } = require("./HomeBannerDesc");
export const {
  default: HomeBannerBackground,
} = require("./HomeBannerBackground");
